import { Test } from "../../../../components/test.tsx";

# Test Obtain Path

Simple LOCATION using `window.location.href` only works locally

Simple PATH using `window.location.pathname` only works locally

# Test Import JSX

<Test />

Using `<Test />` imported from `/components/test.tsx`

# Test ghEditLink

Not actually using `<ghEditLink />` importing from `/components/ghEditLink.tsx` since it throws errors, but that's the idea.
