import { dumbTest } from "../../../../components/test.tsx";
import { ghEditLink } from "../../../../components/ghEditLink.tsx";

import { Link } from "@redocly/developer-portal/ui";

# Test Obtain Path

Simple LOCATION using `window.location.href`:

<strong>{window.location.href}</strong>

Simple PATH using `window.location.pathname`:

<strong>{window.location.pathname}</strong>

# Test Import JSX

<dumbTest />

tried `<dumbTest />` importing from `/components/test.tsx` right above but nothing displays.

# Test ghEditLink

Not actually doing `<ghEditLink />` importing from `/components/ghEditLink.tsx` since it throws errors, but that's the idea.
