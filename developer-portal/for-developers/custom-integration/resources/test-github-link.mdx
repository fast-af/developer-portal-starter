import { GHEditLink } from "../../../../components/ghEditLink.tsx";

# Test Obtain Path

Simple LOCATION using `window.location.href` only works locally

Simple PATH using `window.location.pathname` only works locally

# Test Import JSX

Not using `<Test />` imported from `/components/test.tsx` since it throws errors, but that's the idea.

# Test ghEditLink

<GHEditLink />

Using `<GHEditLink />` importing from `/components/ghEditLink.tsx`
